import React from 'react';
import ChatInterface from '../components/ChatInterface'; // Adjusted path

const ChatPage: React.FC = () => {
  return (
    <ChatInterface />
  );
};

export default ChatPage; 